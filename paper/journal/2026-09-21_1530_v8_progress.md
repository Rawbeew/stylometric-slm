# Journal Entry 9: v8 retrain — live progress (2026-09-21 15:30)

v8 retrain started after v7 succeeded training (91.2% eval acc) but failed at save.
PID 7616. 3 epochs, batch=16, lr=2e-4, max_input_len=256.

## Snags we have hit (from earlier sessions — what to watch for)
1. TPU OOM — n/a (CPU VM)
2. VM OOM — RSS > 50GB would trigger. Watch RSS.
3. Protobuf missing — n/a (torch already installed)
4. Wrong URLs / contaminated passages — caught earlier, data now clean
5. Front-matter leakage (gradient explosion) — fixed; would show as grad_norm > 50
6. mT5 span-corruption pretrain bias — n/a (encoder-only now)
7. Tied-weight safetensors error — fix in place (pickle save)
8. Classifier save_pretrained missing — fix in place (manual encoder.save + torch.save state_dict)

## Snags to watch for in v8 (anomaly triggers)
- PROCESS_DEAD — PID gone
- TRACEBACK_IN_LOG — any uncaught exception
- OOM — torch OOM
- RUNTIME_ERROR — anything else
- TIED_WEIGHT_SAVE_ERROR — if save regresses
- NETWORK_FAILURE — HF push fails
- DISK_FULL — /home/alaga/ fills up
- eval_loss > 5.0 — gradient explosion
- eval_acc < 5% — model not learning (we expect >50% at epoch 1)
- steps_per_sec < 0.05 — process stuck
